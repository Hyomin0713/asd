{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev --package-lock=false && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
